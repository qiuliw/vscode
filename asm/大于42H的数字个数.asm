DATA SEGMENT
    ARRAY DB 100H DUP(?)
    UP DB ?
    DOWN DB ?
DATA ENDS
CODE SEGMENT
    ASSUME CS:CODE,DS:DATA
START:
    MOV DX,DATA
    MOV DS,DX
    MOV BX,0
    LEA SI,ARRAY
    MOV CX,100H
L0:
    MOV AL,[SI]
    CMP AL,42H
    JZ NEXT
    JA L1
    INC BL
    JMP NEXT
L1:
    INC BH
NEXT:
    INC SI
    LOOP L0
    
    MOV UP,BH
    MOV DOWN,BL
    MOV AH,4CH
    INT 21H
CODE ENDS
    END START